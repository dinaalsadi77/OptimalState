<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageView
            android:id="@+id/logo"
            android:layout_width="match_parent"
            android:layout_height="170dp"
            android:layout_marginTop="70dp"
            android:src="@drawable/logo" />

        <TextView
            android:id="@+id/Exercises"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="25sp"
            android:text="Exercises"
            android:textAlignment="center"
            android:textStyle="bold"/>


        <!-- Take Assessment Dropdown List -->
        <Spinner
            android:id="@+id/BreathingExercises"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            android:backgroundTint="#FFC107"
            android:textAlignment="center"
            android:padding="10dp"
            android:spinnerMode="dropdown"/>

        <!-- View Current Exercises Dropdown List -->
        <Spinner
            android:id="@+id/OtherExercises"
            android:name="spinner_prompt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textAlignment="center"
            android:backgroundTint="#FFC107"
            android:padding="10dp"
            android:spinnerMode="dropdown"/>

        <!-- View History Dropdown List -->
        <Spinner
            android:id="@+id/RecommendedFoods"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textAlignment="center"
            android:backgroundTint="#FFC107"
            android:padding="10dp"
            android:spinnerMode="dropdown"/>

        <!-- Settings Dropdown List -->
        <Spinner
            android:id="@+id/WatchVideo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:backgroundTint="#FFC107"
            android:padding="10dp"
            android:textAlignment="center"
            android:spinnerMode="dropdown"/>

    </LinearLayout>
</ScrollView>
